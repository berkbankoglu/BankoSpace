name: Release Build

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  release:
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: 'windows-latest'
            args: '--target x86_64-pc-windows-msvc'
          - platform: 'ubuntu-22.04'
            args: ''
          - platform: 'macos-latest'
            args: '--target universal-apple-darwin'

    runs-on: ${{ matrix.platform }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install dependencies (Ubuntu only)
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.0-dev libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 'lts/*'
          cache: 'npm'

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.platform == 'macos-latest' && 'aarch64-apple-darwin,x86_64-apple-darwin' || '' }}

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: './src-tauri -> target'

      - name: Install frontend dependencies
        run: npm ci

      - name: Build Tauri app
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: 'BankoSpace ${{ github.ref_name }}'
          releaseBody: |
            ## 🎉 BankoSpace ${{ github.ref_name }}

            ### ✨ Otomatik Güncelleme Aktif!
            Bu sürümden itibaren uygulama otomatik güncelleme desteğine sahip.
            - İlk kurulum için installer'ı indirin
            - Sonraki güncellemeler otomatik gelecek!

            ### 📥 İndirme
            Aşağıdan işletim sisteminize uygun installer'ı seçin.

            **Windows Kullanıcıları için:**
            - `.msi` veya `.exe` dosyasını indirin
            - Kurulum sırasında Windows Defender uyarısı çıkarsa "More info" → "Run anyway"

            **Not:** İlk kez yükleyenler bu installer'ı kullanmalı. Mevcut kullanıcılar otomatik güncelleme alacak!
          releaseDraft: false
          prerelease: false
          args: ${{ matrix.args }}
